<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.secondhand.pojo.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="wechat" property="wechat"/>
        <result column="avatar" property="avatar"/>
        <result column="role" property="role"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByUsernameAndPassword" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.secondhand.pojo.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, phone, wechat, role, status)
        VALUES (#{username}, #{password}, #{phone}, #{wechat}, #{role}, #{status})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <update id="update" parameterType="com.secondhand.pojo.User">
        UPDATE user
        <set>
            <if test="phone != null">phone = #{phone},</if>
            <if test="wechat != null">wechat = #{wechat},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- ========== 管理员专用 ========== -->

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM user
        <where>
            role = 0  <!-- 只查普通用户，不包含管理员 -->
            <if test="keyword != null and keyword != ''">
                AND username LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id DESC
    </select>

    <update id="updateStatus">
        UPDATE user SET status = #{status} WHERE id = #{id}
    </update>

    <update id="resetPassword">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>

</mapper>