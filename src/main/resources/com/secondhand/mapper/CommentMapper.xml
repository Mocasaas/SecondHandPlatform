<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhand.mapper.CommentMapper">

    <resultMap id="CommentResultMap" type="com.secondhand.pojo.Comment">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="user_id" property="userId"/>
        <result column="rating" property="rating"/>
        <result column="content" property="content"/>
        <result column="create_time" property="createTime"/>
        <association property="product" javaType="com.secondhand.pojo.Product">
            <id column="p_id" property="id"/>
            <result column="p_title" property="title"/>
            <result column="p_price" property="price"/>
            <result column="p_image" property="image"/>
        </association>
        <association property="user" javaType="com.secondhand.pojo.User">
            <id column="u_id" property="id"/>
            <result column="u_username" property="username"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="com.secondhand.pojo.Comment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO comment (order_id, product_id, user_id, rating, content)
        VALUES (#{orderId}, #{productId}, #{userId}, #{rating}, #{content})
    </insert>

    <select id="selectByProductId" resultMap="CommentResultMap">
        SELECT c.*,
               p.id AS p_id, p.title AS p_title, p.price AS p_price, p.image AS p_image,
               u.id AS u_id, u.username AS u_username
        FROM comment c
                 LEFT JOIN product p ON c.product_id = p.id
                 LEFT JOIN user u ON c.user_id = u.id
        WHERE c.product_id = #{productId}
        ORDER BY c.create_time DESC
    </select>

    <select id="selectByUserId" resultMap="CommentResultMap">
        SELECT c.*,
               p.id AS p_id, p.title AS p_title, p.price AS p_price, p.image AS p_image,
               u.id AS u_id, u.username AS u_username
        FROM comment c
                 LEFT JOIN product p ON c.product_id = p.id
                 LEFT JOIN user u ON c.user_id = u.id
        WHERE c.user_id = #{userId}
        ORDER BY c.create_time DESC
    </select>

    <select id="selectByOrderId" resultMap="CommentResultMap">
        SELECT * FROM comment WHERE order_id = #{orderId}
    </select>

    <delete id="deleteByOrderId">
        DELETE FROM comment WHERE order_id = #{orderId}
    </delete>

</mapper>